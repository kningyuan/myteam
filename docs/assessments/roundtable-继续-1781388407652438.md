# 圆桌最佳实践修正项汇总

> **议题**：认可main的Workflow v3么？
> **群组**：龙虾大本营
> **状态**：BEST_PRACTICE: PASSED（交叉确认投票通过）
> **圆桌记录**：`business/tasks/group_roundtables/g_1781253949_12/m_1781388407652438.md`
> **消息 ID**：`m_1781388407652438`
> **生成时间**：2026-06-14 07:10:04

## 总判定

Workflow v3 骨架正确（P1→P2→P3→P4 + P3 内并行扇出），经下列修正项后可执行。

## 修正项（共 7 项）

### 修正项 1：E2E 测试基线提前至 P1 末尾（三角色一致）

- P1 末尾必须产出**至少 1 条编排内核完整链路的 E2E 测试**，且能"可执行两次"（改造前基线 + 改造后回归）
- Gate：测试通过率 100%，两次执行结果一致

### 修正项 2：token 验收改功能性标准（三角色一致）

- 原"覆盖率 ≥ 80%"无依据，改为：adapter 能提取 + API 能返回 + 前端能展示（三步功能性验收）
- Gate：各 CLI adapter 的 token 数据在 SSE 事件中可验证

### 修正项 3：Store 类冲突采用"先契约后实施"（三角色一致）

- DAG 调整：2.3a（Store 层定义 token 计量抽象接口）→ 2.3b（adapter 层采集实现）+ 2.4（PG 迁移）并行
- 2.3a 输入需包含 2.1 的路由边界图（API 拆分中间产出）
- 2.5（group_manager 直写收拢）产出的"直写清单"作为 2.4 输入

### 修正项 4：竞品对比纳入 P1 末尾（三角色对齐后一致）

- 位置：P1 末尾（诊断完成、方案开始前），标注为"产品定位参考，非技术方案依赖"
- Gate：覆盖 ≥ 3 个竞品（CrewAI、LangGraph、AutoGPT），维度 ≥ 5 个，有量化数据和引用来源
- 产出物：`docs/assessments/competitor-baseline.md`
- 不阻塞 P2 任何技术任务

### 修正项 5：产品验收采用"轻量确认 + 完整验收"双层模式（三角色对齐后一致）

- P3.2 完成后：@product 轻量确认"功能存在"（仅在 P1.1 矩阵上标注"已补齐"，不阻塞 DAG）
- P4 验收：@product 完整验收"功能可用" + "核心旅程可完成"
- 解决 @arch 的 DAG 并行性保护 + @product 的产品职责及时性

### 修正项 6：`_KERNEL_RUNS` 作为 P3.1 附带修复（三角色一致）

- 不纳入 P2 方案阶段，在 3.1（API 拆分）的 task description 中注明
- 产出物包含 `_KERNEL_RUNS` 修复变更日志
- Gate 条件包含"_KERNEL_RUNS 修复验证通过"

### 修正项 7：Gate 条件量化参照 `FRAMEWORK-FREEZE.md` 标准（@research 主张，@arch/@product 同意）

- P1 Gate：P0 链路功能完整度 = 100%，且有代码/测试证据
- P2 Gate：每个方案产出物包含目标状态、当前差距、实施步骤、验收指标、风险评估
- P3 Gate：E2E 测试覆盖 ≥ 3 条关键路径，通过率 100%
- P4 Gate：逐项列出验收清单，避免循环引用

## 来源

由群组圆桌 `_run_group_roundtable` 在共识确认通过后自动整理；内容摘自主持人最佳实践草案中的「修正项」章节。
