# myteam 升级总路线图

**日期**：2026-06-09（L2 闭环更新）  
**愿景**：工业级多 Agent 协作框架——可审计、可重复、能自进化

## 三层演进

### L1 可靠性底座 — ✅ **L1_GATE_PASS**（2026-06-09）

| 里程碑 | 用户可感知 | KPI | 状态 |
|--------|-----------|-----|------|
| rules 注入 | Agent 编排与聊天角色一致 | K2 Gate 首错率 ↓30% | **PASS**（REG-RULES CHECK_ONLY） |
| 配置贯通 | 设置 Tab 参数下一项目生效 | 日志可见 WatchdogConfig | **PASS**（REG-D） |
| 删墙钟 | 长任务不被 600s 误杀 | K3 < 5% | PASS（REG-02 K3=0%） |
| token 计量 | 成本真实可见 | K5 = 100% | PASS（REG-05） |
| 孤儿回收 | 写完交付物崩溃不丢 | K4 ≥ 95% | PASS（REG-04 K4=100%） |

**发版门禁**：REG-02，K1 ≥ 80% — **PASS**（K1=100%）

**文档**：`09-Sprint1-功能预期对齐.md`、`11-Sprint1-交叉验证纪要.md`

**L1 尾项已闭合**：R-K-E 交互级 budget 硬停（`test_execute_budget_exceeded_pauses_mid_interaction` PASS）

---

### L2 协作机制 — ✅ **L2_GATE_PASS**（2026-06-09）

| 里程碑 | 用户可感知 | KPI / 验收 | 状态 |
|--------|-----------|------------|------|
| 波次真并行 | 同波次多叶子同时执行 | K8 ≥ 0.8 | **PASS**（K8=100%） |
| Store 并发加固 | 并行写 Store 不锁死 | 无 `database is locked` | **PASS**（单测） |
| 失败原因透传 | triage 可见 `fail_reason` | 单测 + meta 可检索 | **PASS** |
| K8 度量接入 | 回归脚本输出并行度 | `check_kpis.py` k8_pass | **PASS** |
| Gate retry session | 格式重试续聊 | K2 提升 | **PASS**（REG-K17） |
| R2 死管道清账 | 假绿测试标注/收口 | pytest `r2_infra` 标记 | **PASS**（已标注） |

**发版门禁**：Level-2 验证项目（三角色并行评审）

| 门禁项 | 脚本 / workflow | 阈值 | 状态 |
|--------|-----------------|------|------|
| REG-L2 | `reg_l2_3role.py` + `reg-l2-3role.yaml` | K1≥80%, K8≥0.8 | **PASS** |
| REG-02 parallel | `reg-parallel-4leaf.yaml` + `reg_02_parallel.py` | K1+K3+K8 | **PASS** |
| pytest T1+L2 | 见 `03-QA度量与回归体系.md` | 全绿 | **PASS**（101 passed） |

**文档**：`12-L2-交叉验证纪要.md`

---

### L3 自进化 — ⏳ **L3_SCAFFOLD**（+1~2 月）

| 里程碑 | 用户可感知 | 发版门禁 | 状态 |
|--------|-----------|----------|------|
| Skill 抽提/回读 | 成功模式自动沉淀为 Skill | Level-3 内容评审 | scaffold |
| self-upgrade workflow | 内核/agent 可提案自升级 | 人确认合入 | scaffold |
| Budget 自动降级 | 超预算自动缩 scope/换模型 | REG budget 系列 | 占位 |
| Level-3 场景 | 内容→发布→证据 URL | content-campaign REG | 未开始 |

**发版门禁**：Level-3（内容→发布→证据 URL）

**L3 入口条件**：

- [x] L2 发版门禁 REG-L2 pass
- [ ] K7 triage 有效决策率 ≥ 80%（L2 尾项，可选）
- [x] R2 死管道标注（`test_r2_features.py` pytestmark `r2_infra`）

**L3 首批工作包**（scaffold 已建，运行时未接线）：

1. `business/workflows/self-upgrade.yaml` — plan→execute→review 8 任务
2. `scripts/regression/reg_l3_self_upgrade.py` — CHECK_ONLY prerequisites PASS
3. `docs/0608/13-L3-需求对齐纪要.md` — Phase 0 对齐
4. Budget 自动降级 — `kernel_config.py` 文档占位，Process 未接线
5. Level-3 content-campaign REG — 未建

---

## 当前总状态（2026-06-09）

| 层级 | 门禁标识 | 结论 |
|------|----------|------|
| L1 | `L1_GATE_PASS` | ✅ 达标 |
| L2 | `L2_GATE_PASS` | ✅ 达标 |
| 框架封板 | `L2_FRAMEWORK_SEAL` | ⏳ 进行中（见 [14-L2-框架封板门禁.md](./14-L2-框架封板门禁.md)） |
| L3 | `L3_SCAFFOLD` | ⏳ 骨架就绪，场景 E2E 未交付 |

> **实现真相源**：需求 ID ↔ 代码锚点见 `docs/new/03-需求-实现映射与演进.md`

## 需求满足度（L3 完成后目标）

| 需求 | 满足度 |
|------|--------|
| 多 subagent 协作 | 85% |
| 工业级框架 | 85% |
| AI 自升级 myteam | 80%（人确认合入） |
| 运营小红书 | 55%（发布需外部工具） |
| GEO 优化 | 70% |
| 自动沉淀 skill | 65% |
| 稳步可证明提升 | 85% |

详见 [05-交叉讨论共识.md](./05-交叉讨论共识.md)
