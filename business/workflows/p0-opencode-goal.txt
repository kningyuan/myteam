【P0 产品研发 · OpenCode 试跑】

为 myteam Hub 增加 **workflow 保存前校验** 能力（Owner 决策 Mac App v1-A）。

范围（刻意缩小，便于 OpenCode 试跑）：
1. PRD：说明校验什么（task_type 存在、plan_gate、非法 agent）
2. 架构评审：Hub API 层改动点（server.py + workflow_loader.validate_workflow）
3. 实现：一个最小 Python 函数 `validate_workflow_payload(data) -> list[str]` 返回错误列表（可放在 backend/common/ 或 hub/services/）
4. 测试：1–2 个 pytest 用例（合法/非法 YAML dict）
5. 验收：对照 PRD 检查项

约束：
- 不修改 Process/Gate 内核行为
- 交付物按各 task_type 章节要求
- 代码量控制在 POC 级别（<200 行新代码）
