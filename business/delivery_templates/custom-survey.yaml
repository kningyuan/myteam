id: custom-survey
version: '1.0'
display_name: 自定义调研
description: 自定义调研交付模板
task_types:
  - custom-survey
deliverable_template:
  required_heading_level: 2
  sections:
    - name: 背景
      description: 填写「背景」章节
      required: true
    - name: 结论
      description: 填写「结论」章节
      required: true
check_rules:
  required_sections:
    - 背景
    - 结论
  min_length: 200