id: prd-lite
version: '1.0'
display_name: 轻量PRD
description: 轻量需求文档模板（背景/范围/用户故事与验收标准）
task_types:
  - requirements
deliverable_template:
  required_heading_level: 2
  sections:
    - name: 背景与目标
      description: 填写「背景与目标」
      required: true
    - name: 范围
      description: 填写「范围」
      required: true
    - name: 用户故事与验收标准
      description: 填写「用户故事与验收标准」
      required: true
check_rules:
  required_sections:
    - 背景与目标
    - 范围
    - 用户故事与验收标准
  min_length: 400
